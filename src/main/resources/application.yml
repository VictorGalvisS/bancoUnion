spring:
  jackson:
    time-zone: America/Santiago
    locale: es_CO
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/PruebaTecnicaBancoUnion?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    password: Password123#@!
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL8Dialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
#logging:
#  level:
#    org:
#      hibernate:
#        SQL: debug
#      springframework:
#        security:
#         web:
#           '*': debug